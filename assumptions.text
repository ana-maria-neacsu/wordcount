Iteration #1:
Only Space is counted as a word separator
Words with any other characters other than a-zA-Z should be ignored
Since the input comes from the command line we assume that i's rather short not to care about RAM and therefor we won't be using StringBufferst